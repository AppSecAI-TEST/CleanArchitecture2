<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cleanarchitecture.shishkin">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:name=".application.app.ApplicationController"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme"
        android:windowSoftInputMode="adjustPan"
        android:excludeFromRecents="false"
        android:launchMode="singleInstance"
        android:configChanges="keyboardHidden|orientation|screenSize" >

        <activity android:name=".application.ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".base.controller.NotificationBroadcastReceiver" >
            <intent-filter>
                <action android:name="${applicationId}.action.NotificationBroadcastReceiver.ACTION_DELETE_MESSAGES" />
            </intent-filter>
        </receiver>

        <service android:name=".base.controller.NotificationService"/>
        <service android:name=".base.storage.DiskCacheService"/>
        <service android:name=".base.storage.MemoryCacheService"/>

    </application>

</manifest>